#ifndef MAPPA_H
#define MAPPA_H
#include "stanza.h"

class mappa
{
    public:

        mappa();
        virtual ~mappa();
        creamappa();
        stampamappa();
    protected:
            std::vector<std::vector<stanza*> > strutturamappa;

    private:
};

#endif // MAPPA_H
